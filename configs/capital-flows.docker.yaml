# Domain display-name: "Holder Conviction". The domain_code CAPITAL_FLOWS is
# retained in the DB for schema stability. Components: ETF + LTH (+ Miner stub).
domain: capital_flows
model_version: capital_flows_v1.2.0

providers:
  coinglass:
    api_key: ""
    timeout_sec: 30

features:
  intermediate_version: capflows_int_v3.2.0
  final_version: capflows_final_v3.2.0

database:
  url: postgres://regime:regime@postgres:5432/regime?sslmode=disable

schedule:
  tick_every: 4h
